#include <gtest/gtest.h>
#include "CoreClass/ConfigManager.h"

TEST(ConfigManagerTest, LoadConfig) {
    CoreNS::ConfigManager configManager;
    // Assumendo che esista un file di configurazione di test
    // configManager.load("test_config.json"); 
    // ASSERT_EQ(configManager.get<std::string>("test_key"), "test_value");
}